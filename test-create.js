const fetch = require('node-fetch'); // wait, Next.js provides global fetch

async function run() {
  // we don't have authentication cookies for the admin user from the node script.
  // this will return 401/403.
}
run();
